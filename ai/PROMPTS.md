# Prompts Used (AI Version)

## Prompt
"Build a Python Jupyter notebook for a complete ML workflow on the Iris dataset. The notebook should load the data, split into train/test, train multiple classifiers (Logistic Regression, LDA, KNN, CART, Naive Bayes, SVM), evaluate them with 10-fold cross-validation, compare results with a boxplot, and make predictions on the test set with a confusion matrix and classification report. Include visualizations (boxplots, histograms, scatter matrix)."
