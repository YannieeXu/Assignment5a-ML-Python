REFLECTION -- Assignment 5 (Python)

1. How does the AI model performance compare?
The manual and AI versions achieved similar accuracy (~97%) on the Iris dataset. Both identified SVM and LDA as top performers. Small differences in cross-validation scores are due to random variations in the train/test split, not meaningful model differences.

2. How does the AI project structure compare?
The AI version uses more compact code (list comprehensions, dict iterations) while the manual version follows the tutorial's step-by-step approach. Both are functionally equivalent. The AI structure is more concise but the manual structure is easier to follow for beginners.

3. Would you trust the AI version without doing Part A?
No. Without the manual experience, I would not know what the expected accuracy range should be, how cross-validation works, or whether the AI's model choices make sense. Knowing how to validate the results is more important than the code itself.

4. What would you miss without Part A?
The detailed understanding of each step. The manual version forced me to understand why each function is used, what the parameters mean, and how to interpret the output. The AI version produces correct code but skips the learning process.
